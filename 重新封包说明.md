# TapTap小游戏重新封包工具使用说明

## 使用场景

开发者在 Cocos Creator 构建完成后，可能需要手动修改生成的代码（如调试、临时修复等），然后重新打包上传到 TapTap 平台。

**常见场景**：
- 修改 `build/TapBuild/game/` 目录中的 JS 代码
- 替换资源文件
- 调整配置文件
- 临时测试某些修改

---

## 工具文件

项目根目录包含以下文件：

```
UIDemo/
├── repack_game.py          # Python 重新封包脚本（跨平台）
├── repack_game.bat         # Windows 批处理脚本（英文版，推荐）
└── repack_game.ps1         # PowerShell 脚本（中文版，支持中文）
```

---

## Windows 用户使用方法

### 方法 1：PowerShell 脚本（推荐，支持中文）

1. **右键点击** `repack_game.ps1`
2. 选择 **"使用 PowerShell 运行"**

如果无法运行，可能需要修改执行策略：
1. 以管理员身份打开 PowerShell
2. 执行：`Set-ExecutionPolicy RemoteSigned`
3. 选择 `Y`（是）

### 方法 2：批处理脚本（英文界面）

### 方法 1：推荐方式（自动检测）

1. **复制脚本到构建目录**

   将 `repack_game.bat` 和 `repack_game.py` 复制到：
   ```
   build/TapBuild/
   ```

   目录结构应该是：
   ```
   build/TapBuild/
   ├── game/               # 游戏目录
   ├── game.zip            # 旧的 ZIP 包
   ├── repack_game.bat     # ← 批处理脚本
   └── repack_game.py      # ← Python 脚本
   ```

2. **双击运行**

   直接双击 `repack_game.bat`，脚本会自动检测同级的 `game` 文件夹并开始打包。

### 方法 2：自定义路径

1. **双击运行** `repack_game.bat`

2. **输入路径**

   如果当前目录没有 `game` 文件夹，脚本会提示输入路径：
   ```
   请输入 game 文件夹的完整路径:
   提示: 可以直接拖拽 game 文件夹到此窗口

   请输入路径:
   ```

3. **拖拽文件夹**

   直接将 `game` 文件夹拖拽到命令行窗口，按回车即可。

---

## macOS / Linux 用户使用方法

### 使用 Python 脚本

1. **打开终端**，进入项目根目录

2. **执行脚本**（使用默认路径）：
   ```bash
   python3 repack_game.py
   ```

3. **或指定自定义路径**：
   ```bash
   python3 repack_game.py --game-dir /path/to/game
   ```

4. **调整压缩级别**（可选）：
   ```bash
   python3 repack_game.py --compression 6
   ```

---

## 输出示例

### Windows 输出

```
============================================================
TapTap 小游戏重新封包工具 (Windows)
============================================================

[检测] 找到 game 文件夹: D:\Projects\UIDemo\build\TapBuild\game
[检查] 正在检查 Python 环境...
[检查] 找到 Python 3.11.5
[检查] 找到脚本: repack_game.py

============================================================
开始打包...
============================================================

============================================================
TapTap 小游戏重新封包工具
============================================================
📁 game 目录: D:\Projects\UIDemo\build\TapBuild\game
📦 输出文件: D:\Projects\UIDemo\build\TapBuild\game.zip
🗜️  压缩级别: 9/9
============================================================

📦 开始打包 1523 个文件...
  进度: 100/1523 个文件
  进度: 200/1523 个文件
  ...
  进度: 1500/1523 个文件

✅ 打包完成！
   文件数量: 1523
   ZIP 大小: 15.78 MB
   输出路径: D:\Projects\UIDemo\build\TapBuild\game.zip

🔍 验证 ZIP 内部结构（前 5 个文件）：
   ✓ game.js
   ✓ game.json
   ✓ assets/main/index.js
   ✓ cachedPlugin/UnityPlugin/index.js
   ✓ check-version.js

✓ 目录结构正确（文件在 ZIP 根目录）

============================================================
✅ 全部完成！game.zip 可以上传到 TapTap 开发者中心
============================================================

[成功] 打包完成！

game.zip 已生成，可以上传到 TapTap 开发者中心

按任意键退出...
```

---

## 常见问题

### Q1: 提示"未找到 Python"

**解决方案**：
1. 下载并安装 Python 3.6+：https://www.python.org/downloads/
2. 安装时勾选 **"Add Python to PATH"**
3. 重启命令行窗口

### Q2: 提示"缺少必要文件: game.js, game.json"

**原因**：指定的目录不是有效的游戏目录

**解决方案**：
- 确保指向的是 `game` 文件夹（包含 game.js 和 game.json）
- 不是 `TapBuild` 文件夹
- 不是 `wechatgame` 文件夹

### Q3: ZIP 文件结构不正确

**问题现象**：
- TapTap 平台上传后无法运行
- 提示找不到 game.js

**原因**：
- 直接对 `game` 文件夹右键压缩会产生错误结构

**正确方式**：
- 必须使用本工具重新封包
- 确保 ZIP 内部文件在根目录（不含 `game/` 前缀）

### Q4: 打包速度慢

**解决方案**：
降低压缩级别以提升速度（文件会稍大）：
```bash
python3 repack_game.py --compression 6
```

---

## 技术说明

### ZIP 结构要求

TapTap 小游戏要求 ZIP 包内文件直接在根目录：

✅ **正确结构**：
```
game.zip
├── game.js
├── game.json
├── assets/
│   └── main/
│       └── index.js
└── cachedPlugin/
    └── UnityPlugin/
        └── index.js
```

❌ **错误结构**：
```
game.zip
└── game/          ← 多了一层目录
    ├── game.js
    ├── game.json
    └── ...
```

### 实现原理

脚本使用 Python 的 `zipfile` 模块，通过 `relative_to()` 方法确保文件的 ZIP 内部路径正确：

```python
# 关键代码
relative_path = item.relative_to(game_dir)
zf.write(item, relative_path)
```

这与插件的打包逻辑完全一致：
- **TypeScript**: `archive.directory(targetFolder, false)`
- **Python**: `zf.write(path, path.relative_to(target_folder))`

---

## 反馈与支持

如遇到问题，请提供：
- 操作系统版本（Windows 10/11, macOS, Linux）
- Python 版本（`python --version`）
- 完整的错误日志
- game 目录的结构（`ls -la game/` 或 `dir game\`）
